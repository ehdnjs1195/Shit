<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<!-- 게시판 목록조회  -->
	<select id="selectBoardList" resultType="boardDto">
		<include refid="pagingHeader"></include>
			SELECT ROWNUM,board_num,board_title,board_writer,board_content,board_view_count,board_regdate,
				board_type_code,reply_order_num,reply_depth_num
			FROM board
			<include refid="search"></include>
			ORDER BY board_regdate DESC
		<include refid="pagingFooter"></include> 
	</select>
	
	<select id="countBoard" resultType="int">
		SELECT count(*)
		FROM board
		<include refid="search"></include> 
	</select>
	<!-- 검색조건 sql -->
	<sql id="search">
		<choose>
			<when test="searchOption=='all'">
				WHERE board_title like '%'||#{keyword}||'%'
				OR board_content like '%'||#{keyword}||'%'
				OR board_writer like '%'||#{keyword}||'%'
			</when>
			<!-- 전체 검색이 아닐 경우 -->
			<otherwise>
				WHERE ${searchOption} like '%'||#{keyword}||'%'
			</otherwise>
		</choose>
	</sql>
	
	<sql id="pagingHeader">
		SELECT * 
		FROM (SELECT ROWNUM as rn, A.* FROM(
	</sql>
	<sql id="pagingFooter">
			) A
		)WHERE rn BETWEEN #{start} AND #{end}
	</sql>
</mapper>